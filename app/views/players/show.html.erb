<h1><%= @player.name %></h1>
<h2>Hej</h2>
<!-- test -->
<div class="row">
	<div class="span3">
		<%= image_tag "kits/#{Team.find(@player.team_id).name.downcase!}.png" %>
	</div>
	<div class="span3">
		<h2>Statistik</h2>
		<table class="table table-striped table-bordered table-condensed">
		<tr><td>Namn:</td><td><%= @player.name %></td></tr>
		<tr><td>Namn:</td><td><%= @player.player_stats.find(:all, :conditions => ["player_id LIKE ? AND round LIKE ?", params[:id], 1]).first.goals %></td></tr>
		</table>
	</div>
</div>