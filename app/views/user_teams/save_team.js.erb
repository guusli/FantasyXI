 //var teamplayers = $.parseJSON($("#teamplayers").text());

$("#pitch_alert").html("<%= flash[:notice] %>");
$("#pitch_alert").css('display','block');
$("#pitch_alert").addClass('alert-success');
$("#pitch_alert").removeClass('alert-info');

$('#user_team_name').html("<h1><%= current_user.user_teams[0].name %>");

/*$.each(teamplayers, function(i, position) {
	$.each(position, function(j, player) {
	 	if(player) {
	 		$.get('/change_formation');
	 	}
	});
});*/